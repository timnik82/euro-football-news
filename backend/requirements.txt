# Direct runtime dependencies. Transitive packages (starlette, pymongo, anyio,
# certifi, etc.) are resolved automatically by pip and no longer pinned here.
fastapi==0.110.1
uvicorn==0.25.0
pydantic==2.12.5
python-dotenv==1.2.2
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0
httpx==0.28.1
bcrypt==4.1.3
PyJWT==2.12.1

# Test-only dependencies.
pytest==9.0.2
requests==2.33.1
